<div dir="rtl" style="text-align: right;">

# جرد المخزون (`inventory-audit-updater`)

![version](https://img.shields.io/badge/version-v2.1.0-blue)

**بتعمل إيه:** الموظفين بيراجعوا أرصدة المخزون بالباركود/SKU، يصلّحوا الفرق (`adjust_inventory`)، أو يأكّدوا إن الجرد مظبوط (`set_audit_date`) — مع قايمة أولويات جرد وتفاصيل الأوردرات الغير مجهّزة لكل صنف، و**جرد جماعي** (`§BULK`) لرفوف كاملة بالسكانر في جلسة واحدة.
**مين بيستخدمها:** المخزن
**الإصدار:** Worker `v6.1.0` · الواجهة `v6.1.0`

## الروابط

```
الواجهة    : https://ecommoda-dev.github.io/Inventory-Audit-Updater/
الـ Worker : https://inventory-audit-updater-worker.ecommoda-dev.workers.dev
اسم الـ Worker في الداشبورد: inventory-audit-updater-worker     ← لازم يطابق name في wrangler.toml
```

## الـ Endpoints

| `?action=` | بيعمل إيه |
|---|---|
| `check_employee` | فحص موظف قبل تسجيل الدخول |
| `register_pin` | تسجيل PIN لأول مرة |
| `verify_employee` | تسجيل الدخول (PIN) |
| `log_logout` | تسجيل خروج |
| `get_employees` | قايمة الموظفين النشطين |
| `list_skus` | بحث عن SKU/منتج |
| `lookup_barcode` | بحث بالباركود (سكانر) |
| `get_details` | الأوردرات الغير مجهّزة لصنف — بيرجّع `orderId` الرقمي كمان |
| `get_audit_date` | آخر تاريخ/موظف/ملاحظات جرد لصنف |
| `get_audit_priorities` | قايمة أولويات الجرد + `truncated` لو عدّى السقف |
| `get_bulk_index` | **جماعي** — باركودات كل المتغيّرات الصالحة للجرد + أرصدة **مبدئية** + `dupBarcodes` + `truncated` + السقوف |
| `bulk_compare` | **جماعي** — الأرصدة الحيّة **والمغلَّف** في نداء واحد ولحظة واحدة (POST) + `at` |
| `bulk_adjust` | **جماعي** — الكتابة الجماعية (POST) بقيمة مطلقة + `changeFromQuantity` |
| `adjust_inventory` | تصحيح الرصيد (POST) + تسجيل تاريخ الجرد |
| `set_audit_date` | تأكيد إن الجرد مظبوط من غير تعديل (POST) |
| `get_logs` | صفحة واحدة من السجل (100 صف) — فلترة وترتيب server-side |
| `get_logs_count` | العدد الكلي المطابق للفلاتر |
| `get_logs_export` | التصدير لحد `LOG_EXPORT_MAX` + `{cap, total, truncated}` |
| `diag` | فحص ذاتي بدون كتابة (env · D1 · OAuth · الصلاحيات · LOCATION_ID · التوقيت · CORS) |
| `get_config` | نسخة الـ Worker — الواجهة بتقارنها بـ `MIN_WORKER_VERSION` |

## D1

```
tool  : inventory_audit
type  : ok · adjustment · login · logout
```

> القيم دي مسجّلة بالفعل في جدول D1 في `ecommoda-constants` §7.
> ⛔ **ممنوع أي `writeLog` بقيمة `type` مش في السطر ده** قبل ما تتسجّل هناك.

### نتيجة العملية — `extra.result`

من v5.0.0 كل صف كتابة بياخد `extra.result` (المفردات الرسمية → `constants` §12):

| القيمة | معناها في الأداة دي |
|---|---|
| `success` | التعديل/التأكيد تم واتأكد من شوبيفاي |
| `warning` | **الكمية اتعدّلت فعلاً** لكن تاريخ الجرد ما اتكتبش — مراجعة يدوية، **ومتعيدش التعديل** |
| `error` | النداء وصل لشوبيفاي واترفض — مفيش أي أثر على المخزون |

> `already` و`rejected` **مش منطبقين هنا** — كل نداء كتابة في الأداة دي بيحاول
> فعل حقيقي، مفيش مسار بيتوقف قبل المحاولة.

## الجرد الجماعي (`§BULK` — من v6.0.0)

> القسم ده **مضاف جنب** الأقسام القديمة، ومش بيلمس ولا واحد منها.

### 🔴 الأرقام بتتقرا لحظة المقارنة، مش لحظة الفهرس (من v6.1.0)

```
① حمّل الباركودات  →  خريطة الباركود (ثابتة) + أرصدة **مبدئية** للعرض
② امسح             →  صفر إنترنت · تأكيد لكل قطعة · الغلط بيتمسك قدام الرف
③ قارن بشوبيفاي    →  🔴 الأرصدة الحيّة + المغلَّف في **نفس النداء ونفس اللحظة**
④ نفّذ
```

**ليه الفصل ده:** الفهرس فيه حاجتين بخصائص مختلفة تمامًا:

| | بيتغيّر إمتى | تحميله بدري مشكلة؟ |
|---|---|---|
| **خريطة الباركود** | لما حد يضيف منتج أو يعدّل باركود — نادر | لأ |
| **الأرصدة** | مع كل بيعة وكل شحنة — مستمر | **أيوه** |

الفهرس بيتحمّل **قبل** العدّ عشان المسح يبقى فوري وذكي (تأكيد لكل مسحة ·
الباركود المجهول والمتكرر بيتمسكوا **وإنت واقف قدام الرف**، وهي اللحظة الوحيدة
اللي الغلطة فيها قابلة للتصحيح). لكن أرقامه بتقدم أثناء الجلسة.

الكتابة نفسها محميّة بالـ CAS، فالخطر **مكانش على المخزون** — كان إن **الشاشة
تعرض رقم قديم والموظف ياخد قرار عليه**. عشان كده:

- أي صف ما اتقارنش بياخد حالة `not_compared`، ورقمه بيتعرض بعلامة **«؟»**
  ورمادي، و**التنفيذ عليه مقفول**.
- فشل المقارنة على صنف = `compare_failed`، **مش صفر** — ومش بيتنفّذ.
- بعد **١٥ دقيقة** من المقارنة بيظهر تنبيه، ونافذة التأكيد بتقول الأرقام
  اتسحبت من كام دقيقة.
- إعادة تحميل الباركودات **بتمسح المقارنة** — الصفوف بترجع «لسه ما اتقارنش».

> ⚠️ **وده قفل عيب أدق كان في v6.0.0:** الأرصدة كانت من لحظة الفهرس والمغلَّف
> من لحظة المراجعة — يعني `available + committed − المغلَّف` بتتجمّع من
> **وقتين مختلفين ومش متسقة مع نفسها**. دلوقتي الاتنين من نفس النداء، و`at`
> بترجع في الرد.

> ℹ️ **استعلام الأوردرات الغالي بيتنفّذ للمتغيّرات اللي `committed > 0` بس** —
> الصنف بلا حجز مافيش منه قطع متغلّفة أصلاً، فاستعلام أوردراته إهدار.

### معادلة «المتوقع على الرف»

```
المتوقع = available + committed − المغلَّف
```

نفس معادلة تاب الجرد الفردي بالحرف (`currentShelfQty = available + unpackedCommitted`)
— القطعة المحجوزة لأوردر **لسه ما اتغلّفش** لسه على الرف، واللي اتغلّفت لأ.

🔴 **والمغلَّف بيجي من `bulk_compare` مع الأرصدة في نفس اللحظة.** من غير الخطوة
دي كل صنف عليه أوردرات معلّقة كان هيطلع عجز وهمي.

### التلات أوضاع — وده أخطر قرار في القسم

| الوضع | الصنف اللي **ما اتمسحش** معناه |
|---|---|
| `scanned` (الافتراضي) | **مش جزء من الجرد ده** → مايتلمسش خالص |
| `siblings` (+ باقي المقاسات) | **لو المنتج اتمسح منه مقاس واحد على الأقل** → باقي مقاساته «مش متسكن». المنتج اللي ما اتلمسش خالص **مش بيظهر** |
| `full` (جرد شامل) | **مش موجود على الرف** → رصيده يتصفّر |

#### 🔴 `siblings` بيجمّع بـ `productId` — ودي حقيقة عن المتجر مش اختيار تصميم

في المتجر ده **كل لون منتج مستقل في شوبيفاي، والمتغيّرات هي المقاسات**:

```
NB1906 / Black / 41..45  →  gid://shopify/Product/9580188467522
NB1906 / White / 41..45  →  منتج تاني خالص
```

يعني `productId` **هو** اللون. التجميع بيه بيدّي «باقي مقاسات نفس اللون»
بالظبط — **من غير أي تخمين على أسماء الأوبشنز** (`Size` · `المقاس` · …)
و**من غير خطر إن لون ما اتلمسش يتفلت** مع اللون اللي اتجرد.
نفس التجميع اللي زرار «باقي مقاسات هذا المنتج» شغّال بيه من v3.0.0.

> ⚠️ لو المتجر اتغيّر يومًا وبقى اللون **أوبشن جوّه منتج واحد**، التجميع ده
> هيوسّع النطاق لكل الألوان. صفوف «مش متسكن» **مش متحدّدة افتراضيًا** فمفيش
> كتابة بتحصل لوحدها، بس البند لازم يتراجع ساعتها.

#### قواعد مشتركة

- صفوف `missing` **مش متحدّدة افتراضيًا** في `siblings` و`full`، ونافذة
  التأكيد بتقول عددها بالرقم.
- **`missing` مشروطة بإن المتوقع > 0.** المقاس اللي شوبيفاي بتقول عنه صفر
  والرف فاضي = `match`، وبيتسجّل إنه اتراجع فعلاً. `full` بيتخطّى الأصناف
  الصفر من الأساس (وإلا الجدول = المتجر كله)، و`siblings` بيضمّها لأن نطاقه
  صغير والتسجيل ده هو نص فايدة جرد الرف.
- **الفهرس المقصوص بيقفل `full` بالكامل** — جرد شامل على فهرس ناقص ادّعاء مش
  جرد. **`siblings` مابيتقفلش** (نطاقه اللي اتمسح فعلاً، مش ادّعاء شمول)، بس
  اللافتة بتنبّه إن ممكن يكون فيه مقاسات مش في الفهرس.
- الصنف **بدون باركود** له حالة مستقلة (`no_barcode`) ومابيتصفّرش — هو أصلاً
  مايتجردش بالسكانر.
- الصنف اللي `onHand ≠ available + committed` (حالات مخزون تانية: تالف · فحص
  جودة · محجوز) بيتشال من التنفيذ — المعادلة مابتغطّيهوش.
- **الترتيب الافتراضي للجدول: المنتج ثم المقاس** (رقميًا) — عشان مقاسات المنتج
  الواحد تقرا ورا بعض. ده «ترتيب البيانات الأصلي» اللي الضغطة التالتة على أي
  عمود بترجّع له.

### الكتابة: مطلقة مش نسبية

| | الجرد الفردي | الجرد الجماعي |
|---|---|---|
| الميوتيشن | `inventoryAdjustQuantities` | `inventorySetQuantities` |
| القيمة | فرق **نسبي** | رقم **مطلق** + `changeFromQuantity` (CAS) |
| السبب في شوبيفاي | `correction` | `cycle_count_available` |
| إعادة التنفيذ | **بتضاعف التعديل** ⛔ | بتترفض بلا أي أثر ✅ |

**ليه:** في الجرد الجماعي بيعدّي وقت طويل بين العدّ على الرف والضغط على «نفّذ».
مع الفرق النسبي، بيعة واحدة في النص معناها إن التعديل بيتطبّق على رصيد تاني —
**غلط صامت**. مع الـ CAS شوبيفاي بترجّع `CHANGE_FROM_QUANTITY_STALE`
و**مفيش أي أثر على المخزون**، والصنف بيرجع للموظف برسالة بتقوله يحدّث الأرصدة.

### قيم السجل

⛔ **مفيش أي قيمة `type` جديدة** — الجماعي بيكتب تحت `adjustment` (فيه فرق) و
`ok` (مطابق)، زي الفردي بالظبط. الفرق في `extra`:

```
extra.mode    = 'bulk'
extra.batchId = 'bulk-<base36>'   ← بيلمّ جلسة التنفيذ الواحدة
extra.counted · extra.expected · extra.available · extra.committed · extra.packed
extra.stale   = true لو شوبيفاي رفضت لتغيّر الرصيد
```

> استعلام صفوف الجرد الجماعي:
> ```sql
> SELECT * FROM logs WHERE tool = 'inventory_audit'
>   AND json_extract(extra,'$.mode') = 'bulk'
>   AND json_extract(extra,'$.result') = 'success';
> ```

### سلسلة السقوف

```
① الواجهة  BULK_CHUNK           = 10  ← وقت: نداءين/صنف ≈ ٠٫٩ ث → ٩ ث للنداء
② الـ Worker BULK_MAX_BATCH      = 50  ← حارس لصق
③ شوبيفاي   —                        ← ميوتيشن لكل صنف لوحده (مش دفعة):
                                        `inventorySetQuantities` بترفض الدفعة
                                        كلها لو صنف واحد جواها stale
④ المقارنة BULK_COMPARE_MAX_BATCH = 30 · الواجهة BULK_COMPARE_CHUNK = 12
   ← الوقت للعنصر غير متجانس: ٠٫٠٥ ث بلا حجز · ~٠٫٨ ث معاه.
     السقف محسوب على **الأسوأ**: 10 ÷ 0.8 ≈ 12
```

🔴 **السقوف بترجع للواجهة من `get_bulk_index`** (`chunkHint`/`maxBatch`/`compareChunkHint`/…) —
الواجهة مابتكتبش أرقامها عندها عشان مايبقاش فيه مصدرين بيتعتّقوا.

### توأم لازم يفضل متزامن

`getShelfBreakdown` (§BULK) و`getUnfulfilledOrders` (الفردي) بيحسبوا **نفس**
منطق التغليف بالحرف — الفرق إن الأول بيشتغل على المنتج كله مرة واحدة ويرجّع
مجاميع. **أي تعديل على المنطق ده لازم يتعمل في الاتنين معًا**، وإلا نفس الصنف
يدّي «متوقع» مختلف في التابين بلا أي رسالة.

---

## المضبوط فعليًا في الداشبورد

> اللي **متظبط بالفعل** — مش اللي المفروض يكون.

```
Bindings : DB → ecommoda-dev-logs
Secrets  : WORKER_SECRET · CLIENT_ID · CLIENT_SECRET
Vars     : SHOP_DOMAIN · LOCATION_ID   ← من [vars] في wrangler.toml
Build watch paths : * (الافتراضي — لسه ما اتضيّقتش، §13-ب مفتوح)
```

## CORS

`ALLOWED_ORIGINS` صارمة (`https://ecommoda-dev.github.io` بس) — لأن الأداة كتابة (بتعدّل رصيد المخزون فعليًا).

> 🔴 **تنظيف تم أثناء النقل (27-08-2026):** الكود المنشور قبل النقل كان فيه
> كمان `https://ecommoda24.github.io` (حساب/دومين مهجور) في `ALLOWED_ORIGINS` —
> مذكور صراحةً في `ecommoda-constants` §11 بند 10 كتنظيف كود مطلوب. اتشال في
> `index.js` أثناء النقل. **قرار محتاج تأكيد أحمد** (مش تخمين بلا سند — الشيل
> موثّق في السكيل، بس التنفيذ حصل هنا لأول مرة).

## خط الأساس

> من D1 مباشرة (آخر صفوف نجاح **قبل** النقل — 27-08-2026):

```
login      : 196 صف · آخر واحد 2026-08-27T12:23:45Z
adjustment : 675 صف · آخر واحد 2026-08-24T12:39:23Z
ok         : 920 صف · آخر واحد 2026-07-29T09:16:42Z
logout     : 4 صف   · آخر واحد 2026-07-28T07:47:39Z
```

### ⚠️ استعلام خط الأساس — الصيغة الصحيحة

الاستعلام اللي بيعدّ `type` لوحده **بيقيس المحاولات مش الكتابة الفعلية**
(`constants` §12). من v5.0.0 الأداة بتسجّل الفشل كمان، فالصيغة الصح:

```sql
-- ✅ الكتابة الفعلية
SELECT COUNT(*) FROM logs
 WHERE tool = 'inventory_audit' AND type = 'adjustment'
   AND (json_extract(extra,'$.result') = 'success'
        OR json_extract(extra,'$.result') IS NULL);   -- الصفوف قبل v5.0.0 نجاح بالتعريف

-- ❌ بيقيس المحاولات
SELECT COUNT(*) FROM logs WHERE tool = 'inventory_audit' AND type = 'adjustment';
```

> الصفوف الأقدم من v5.0.0 مالهاش `extra.result` — الأداة وقتها ماكانتش بتسجّل
> إلا عند النجاح، فغياب الحقل معناه نجاح. **والواجهة بتعرضها «—» مش «✓»** لأن
> على مستوى الصف الواحد إحنا فعليًا مش عارفين.

## فخاخ الأداة دي

- **الأداة بتعتمد على `LOCATION_ID` في `adjustInventory`** — لو ضاع من
  `[vars]`، `requireLocationId()` بترمي برسالة باسمه **قبل** أي كتابة
  (من v5.0.0). قبل كده كان بيتحوّل لـ `gid://.../undefined` ويفشل جوّه
  الميوتيشن. برضه لازم يتأكد من الداشبورد بعد أي نقل.
- **`delta` نسبي مش مطلق.** إعادة تنفيذ نفس التعديل بتضاعفه. عشان كده نتيجة
  `warning` بتقول صراحةً «متعيدش التعديل» — الكمية اتحركت خلاص.
- **رابط الواجهة بقى constant في الكود** (`WORKER_URL` في `§CONFIG`) — مش في
  `localStorage` ومش حقل إعدادات. الموظف اللي عنده `stockaudit_worker_url`
  قديم محفوظ في المتصفح مش هيتأثر، القيمة بقت متجاهلة.
- **السر الوحيد في `localStorage`:** `stockaudit_worker_secret` — وبس.
- **جلسة الجرد الجماعي كلها في الذاكرة** — أي refresh بيضيّعها. ده مقصود
  (مفيش عدّ بيتخزّن في `localStorage`)، بس لازم الموظف يعرفه.
- **الفهرس والمقارنة الاتنين بياخدوا أرقامهم من `LOCATION_ID` صراحةً**
  (`inventoryLevel(locationId:)`)، مش `inventoryLevels(first: 1)` زي باقي
  الأداة. الفرق مالوش أثر في متجر بموقع واحد، لكن في الجماعي معناه مئات
  التعديلات محسوبة على أرقام موقع تاني.
- **أرقام الفهرس مبدئية بحكم التصميم** — للعرض وإنت بتمسح بس. أي قرار تنفيذ
  بيتبنى على أرقام `bulk_compare` وبس.

## استرجاع النسخ القديمة

> ده بديل الـ tags — دفع الـ tags ممنوع من جلسات Claude Code السحابية.

```
النسخ المرقّمة القديمة (2.1.html · 2.5.html · 3.html · 4.html) و Index.html
القديمة (v3.1.0) محفوظة في commit e33b484 (آخر commit قبل تنظيف الواجهة).
git show e33b484:2.1.html
git show e33b484:2.5.html
git show e33b484:3.html
git show e33b484:4.html        ← ده نفس محتوى index.html بتاع v4.0.1
git show e33b484:Index.html    ← كان v3.1.0 قبل التحويل لصفحة تحويل

نسخة ما قبل مراجعة 12-09-2026 (Worker v4.0.0 · الواجهة v4.0.1):
git show 7f0cce9:index.js
git show 7f0cce9:index.html
```

## بصمة المهارات

| المهارة | الإصدار وقت آخر تعديل |
|---|---|
| ecommoda-worker-builder | v3.3.0 |
| ecommoda-html-builder | v7.0.0 |
| ecommoda-constants | v2.1.0 |
| shopify-graphql-helper | v1.0.0 |

آخر مطابقة: 18-09-2026 · `index.js` v6.1.0 · `index.html` v6.1.0
🔴 معلّقة: — لا شيء

## مسائل مفتوحة

### ✅ Flow Companion — اتشال بالكامل (12-09-2026)

الأداة كان فيها زرار **«✅ Fulfill Orders»** في كارت الأوردرات غير المشحونة:
تحدّد أوردرات، والأداة تبعت trigger لـ Flow Companion (`specifier: FULFILL`)
**من متصفح الموظف مباشرةً**، بتوكن في `localStorage`، و**بدون أي صف في D1**.

**اتشال نهائيًا في `index.html` v5.1.0** — الزرار وخانات التحديد وحقل التوكن
والدوال كلها، والمفتاح القديم `stockaudit_flow_token` بيتمسح تلقائيًا من متصفح
كل موظف عند أول فتح.

**سبب القرار (أحمد، 12-09-2026):** الميزة **مكانتش بتتستخدم**، و**مكانش المفروض
تكون موجودة** — الأداة دي أداة جرد، والـ fulfillment مرحلة شحن. مخالفة القاعدة ١
في `ecommoda-worker-builder` (أداة واحدة = وظيفة واحدة).

**ملاحظات للأرشيف:**

- الميزة كانت موجودة من **أقدم نسخة محفوظة** (`2.1.html`) وعمرها ما اتوثّقت —
  لا في الـ Changelog ولا هنا، والـ Worker ماكانش يعرف عنها حاجة.
- **مافيش طريقة نعرف كانت بتتستخدم قد إيه** لأنها ماكانتش بتكتب في D1 أصلًا.
  دي حلقة مقفولة: الحاجة الناقصة هي نفسها اللي بتمنع الإجابة. لو ظهر بعدين إن
  حد محتاجها، **تتبني من الصفر في أداة الشحن** مش هنا.
- **مفيش قيمة `type` جديدة اتسجّلت** في `ecommoda-constants` §7 — الحذف قفل
  البند من غير ما يلمس المهارة المشتركة.
- كارت الأوردرات غير المشحونة **باقي زي ما هو للعرض** — هو معلومة مهمة وقت
  الجرد (بيحسب `currentShelfQty`). اللي اتشال هو **الفعل** بس.
- استرجاع الكود لو احتاجت: `git show fe50cf8:index.html`

### ✅ الجرد الجماعي — اتضاف (18-09-2026)

تاب رابع (`§BULK`) للمسح المتواصل والمقارنة والتنفيذ الجماعي. التفاصيل في
قسم «الجرد الجماعي» فوق. **الأقسام القديمة ما اتغيّرتش** — التعديل الوحيد
عليها سطر في `switchMainTab` وتعليق توثيقي فوق `getUnfulfilledOrders`.

**تعديل 18-09-2026 (v6.1.0):** الأرقام بقت بتتقرا لحظة المقارنة مش لحظة
الفهرس — التفاصيل في القسم فوق. `bulk_shelf_check` اتشال واتحوّل لـ
`bulk_compare`.

**بنود مفتوحة على القسم ده:**

- **`inventorySetQuantities` أول استخدام في الستاك** — باقي الأدوات كلها على
  `inventoryAdjustQuantities`. اتجرّب بـ fetch مزيّف (٣٠ تأكيد) لكن **لسه
  مااتجرّبش على شوبيفاي حيّة** — أول جلسة حقيقية تتعمل على أصناف قليلة.
- **حجم رد `get_bulk_index`** — متجر بـ ١٠ آلاف متغيّر ≈ ٣ ميجا JSON في نداء
  واحد. شغّال، بس لو المتجر كبر يتقسّم على صفحات.
- **الصفوف الجماعية مالهاش فلتر مستقل في تاب السجل** — بتتلاقى بالبحث في
  الملاحظات («جرد جماعي») أو بـ `json_extract(extra,'$.mode')` في D1 مباشرةً.

### باقي المفتوح

- **Build watch paths لسه على `*` الافتراضي** — ممكن يتضيّق لاحقًا لـ
  `index.js` + `wrangler.toml` زي §13-ب في playbook النقل، مش إلزامي دلوقتي.
- **`ALLOWED_ORIGINS`** — بند التنظيف فوق لسه محتاج تأكيد أحمد.
- **أي الملف كان فعليًا الأحدث قبل النقل** (`4.html` v4.0.1) اتفرض إنه هو
  المصدر الصحيح لـ `index.html`. `Index.html` (v3.1.0) كان أقدم وتحوّل لصفحة
  تحويل بدل ما يتشال — **قرار محتاج تأكيد أحمد**.

## التحقق قبل أي تسليم

```bash
# الـ Worker
node --check index.js

# الواجهة — التلاتة مع بعض، ولا واحد بديل عن التاني
node <skill>/scripts/css-check.js index.html      # Step 9A — البارسر
node <skill>/scripts/js-undef-check.js index.html # Step 9B — الربط
# + فحوص Step 9 بالـ grep (z-index · التوكنز · الكلاسات المهجورة)
```

> ⚠️ **وحارس نسخة الـ Worker يتجرّب في الاتجاهين**: مرة بـ
> `MIN_WORKER_VERSION` الصح (التحذير مختفي) ومرة برقم أعلى من نسخة الـ Worker
> (التحذير يظهر). الاختبار السلبي لوحده بيقول إن الحارس **ساكت**، مش إنه شغّال.

---

آخر تحديث: 18-09-2026

</div>
