<div dir="rtl" style="text-align: right;">

# تقرير فحص الأداة مقابل المهارات — 12-09-2026

![version](https://img.shields.io/badge/version-v1.0.0-blue)

فحص شامل لـ `index.js` (Worker v4.0.0) و`index.html` (الواجهة v4.0.1) مقابل:

| المهارة | بصمة الأداة | الإصدار الحالي | الفجوة |
|---|---|---|---|
| `ecommoda-worker-builder` | v1.0.0 | **v3.0.0** | ٢ major |
| `ecommoda-html-builder` | v1.0.0 | **v7.0.0** | ٦ major |
| `ecommoda-constants` | v1.2.0 | **v2.1.0** | ١ major |

> الأداة اتنقلت لـ Git في 27-08-2026 **بالكود زي ما هو** — يعني هي فعليًا على
> قواعد ما قبل UI Standard v2 كله. البصمة دي مش خطأ تسجيل: الكود فعلاً على
> القواعد القديمة.

**الحصيلة:** ٧ بنود 🔴 (منهم ٤ بيسيبوا أثر غلط على مورد حقيقي أو على السجل) ·
٨ بنود 🟠 (بنية تحتية إلزامية ناقصة) · ~٢٢ بند 🟡 (مطابقة معايير الواجهة).

---

## 🔴 القسم الأول — بنود بتنتج فعل غلط أو رقم كاذب

### 🔴 ١. `adjust_inventory`: المخزون بيتحرّك، والموظف بيتقاله «فشل»، والسجل فاضي

`index.js` — `if (action === 'adjust_inventory')`

```js
const [adjustResult, auditResult] = await Promise.all([
  adjustInventory(env, token, inventoryItemId, deltaInt),   // ← لا رجعة فيه
  setAuditMetafields(env, token, variantId, employee, notes),
]);
await writeLog(env.DB, { … });                               // ← بره أي حماية
```

**تلات أعطال في تلات سطور:**

1. **`Promise.all` بتشغّل الفعل اللي مالوش رجعة مع خطوة تكميلية بالتوازي.**
   لو `setAuditMetafields` رمت (تعريف الميتافيلد اتغيّر · صلاحية ناقصة ·
   `userError`)، الـ `Promise.all` بترمي — **بعد** ما `inventoryAdjustQuantities`
   نجحت فعلاً. الـ handler بيقع في `catch` ويرجّع `500` → الواجهة بتعرض
   **«خطأ: …»** بالأحمر.
2. **الـ `writeLog` ما بيوصلهاش التنفيذ أصلاً** — فالتعديل اللي حصل على شوبيفاي
   **مالوش أي صف في D1**. السؤال «الأداة عدّلت ولا لأ؟» مالوش إجابة بعد ما
   الشاشة تتقفل (مخالفة قاعدة عدم الضياع — Step 5A ⑭).
3. **`delta` نسبي مش مطلق.** الموظف اللي شاف أحمر بيعيد المحاولة — والتعديل
   **بيتطبّق مرتين**. تصحيح `-3` بيبقى `-6`.

هذا بالظبط الفشل اللي `Step 5A ⑩②` اتكتب عشانه: الفشل **بعد** الفعل الأساسي
= `warning` مع إجراء الإصلاح، **مش** `error`.

**الإصلاح المطلوب:** تسلسل مش تواز · `actions[]` بتتملي أول بأول ·
`setAuditMetafields` في `try/catch` بتدّي `warning` بنصّ «الكمية اتعدّلت، لكن
تاريخ الجرد ما اتكتبش — سجّله يدويًا» · `writeLog` في `try/catch` بترجّع
`logged:false` · والصف بيتكتب **قبل** ما الرد يرجع مهما كانت النتيجة.

---

### 🔴 ٢. `shopifyGQL` ناقصة تلات حراسات

`index.js` — `async function shopifyGQL(...)` بتنتهي بـ `return res.json();`

| الناقص | الأثر الفعلي |
|---|---|
| فحص `res.ok` | 401 بترجّع `{"errors":"[API] Invalid API key"}` — **نص مش مصفوفة**. والقارئ بيعمل `data.errors[0].message` → `undefined` → `throw new Error(undefined)` → الموظف بيشوف **«خطأ: undefined»** |
| فحص الرد مش JSON | 429/5xx بترجّع HTML → `res.json()` بترمي `SyntaxError` برسالة ملهاش علاقة بالسبب |
| إعادة محاولة `THROTTLED` | `get_audit_priorities` بيسحب كل الـ variants بالصفحات — أول `THROTTLED` بيقتل النداء كله بدل ما يتأخّر ثانية ويكمّل |
| فحص ③ (تأكيد الـ payload) | `setAuditMetafields` بتفحص `userErrors` بس. `userErrors: []` معناها «مفيش اعتراض» مش «اتنفّذت» — رد بـ `metafields: []` بيعدّي كنجاح |

**الإصلاح:** استبدال الدالة بنسخة Step 5A ① كاملة (بترمي على: فشل شبكة ·
status · رد مش JSON · `data.errors` · `data` فاضية + backoff على `THROTTLED`)،
وإضافة الفحص ③ لكل ميوتيشن.

---

### 🔴 ٣. تاب السجل: السقف ٥٠٠ صف، والشاشة بتقول رقم تاني

الواجهة بتطلب `get_logs?limit=5000` — والـ Worker بيعمل `Math.min(limit, 500)`.
يعني **٥٠٠ صف هي كل اللي بيوصل، من غير أي إشارة**.

خط الأساس المسجّل في `CLAUDE.md`: **١٧٩٥ صف** لهذه الأداة
(`ok` ٩٢٠ · `adjustment` ٦٧٥ · `login` ١٩٦ · `logout` ٤).

| الشاشة بتقول | الحقيقة |
|---|---|
| شبشة **«إجمالي: N»** — محسوبة من الـ ٥٠٠ الواصلة بعد شيل login/logout | العدد الحقيقي ~١٥٩٥ — الرقم المعروض **كاذب** |
| **«× N جرد»** جنب كل SKU (`buildSkuCountMap`) — بتعدّ في نفس الـ ٥٠٠ | بتقلّل عدد مرات الجرد الحقيقي لكل صنف |
| السجل الأقدم من آخر ٥٠٠ عملية | **مش موجود على الشاشة خالص ومفيش صفحات** |
| **«تم تصدير N عملية ✓»** (خضرا، غير مشروطة) | الملف مقصوص عند نفس السقف — **Standards #30 بالحرف** |

**وبند بيخلّي المشكلة أسوأ:** `login`/`logout` **مش مستثنيين في SQL** — بيتشالوا
في المتصفح. يعني ٢٠٠ صف من الـ ٥٠٠ الواصلة بيتحرقوا على صفوف دخول/خروج
ويتحذفوا بعد ما وصلوا.

**الإصلاح:** نموذج `Log Filter Model v2` كامل — `get_logs` (١٠٠/صفحة) +
`get_logs_count` + `get_logs_export` بـ `{cap, total, truncated}` · الفلترة
**كلها** server-side بقوايم (`employees` · `types`) + `dateFrom`/`dateTo` ·
`type NOT IN ('login','logout')` في الـ SQL · بانر `#logExportWarn` ثابت فوق
الجدول ورسالة النجاح في فرع `else` **بس**.

---

### 🔴 ٤. `toCairo` بإزاحة ثابتة ٣ ساعات

`index.html` — `function toCairo(iso){return new Date(new Date(iso).getTime()+3*60*60*1000);}`

التوقيت الصيفي في مصر بيخلص **29-10-2026**. من اليوم ده:

- كل وقت معروض في السجل وفي «آخر جرد» **بيغلط بساعة**
- فلتر «من تاريخ / إلى تاريخ» بيزحف ساعة (وهو أصلًا بيقارن بـ
  `e.timestamp.substring(0,10)` = تاريخ **UTC** بينما العرض بالقاهرة — فرق
  التلات ساعات بيحط عملية بعد ٩ مساءً في يوم تاني)
- اسم ملف التصدير بيستخدم `toISOString()` = **UTC** — تصدير الساعة ١ صباحًا
  بياخد تاريخ إمبارح

**الإصلاح:** نسخ دوال `Intl` من `ecommoda-constants` §13 **بالحرف**، و**نفس
النسخة في الـ Worker وفي الواجهة**. بعدها يوم 29-10-2026 مش محتاج أي تدخل.

---

### 🔴 ٥. `loginAlert` جوّه `.pin-zone` المخفية

`index.html` — صندوق الخطأ مكتوب **داخل** `<div class="pin-zone" id="pinZone">`،
والـ zone دي `display:none` لحد ما الموظف يختار اسمه.

يعني أي فشل في **الخطوة الأولى** — CORS · سر غلط · الشبكة · الـ Worker واقع —
بيتكتب في عنصر **مخفي**. الموظف بيشوف شاشة دخول بقائمة فاضية **من غير أي سبب
معروض**. ده الباج الموثّق حرفيًا في Standards #8 (اتصلّح في إدارة الموظفين
v2.1.2 ولسه موجود هنا).

**الإصلاح:** نقل `#loginAlert` بره `.pin-zone`، تحت `.login-field` مباشرةً.

---

### 🔴 ٦. Flow Companion: كتابة على أوردرات حقيقية من المتصفح، بره الـ Worker

`index.html` — `doFulfillOrders()`

```js
const res = await fetch('https://flow-companion.mivicle.app/rest/1/flow/start/order', {
  method:'POST', body: JSON.stringify({ bearerToken: flowToken, itemId: orderId, specifier:'FULFILL' })
});
if (res.ok) s++; else f++;
```

**ستة بنود مكسورة في نداء واحد:**

1. **`fetch()` خام من كود الأداة** — ممنوع، كل النداءات تعدّي على `apiRequest`
2. **سر تاني في `localStorage`** (`stockaudit_flow_token`) — `WORKER_SECRET` هو
   **السر الوحيد** المسموح يتخزّن جهة العميل
3. **صفر تسجيل في D1** — فعل بيغيّر حالة أوردرات على شوبيفاي ومالوش **ولا صف**
4. **النجاح متقاس بـ `res.ok` بس** — مفيش أي دليل إن الـ Flow اشتغل فعلاً
   (مخالفة القاعدة ٦: «مفيش نجاح من غير دليل»)
5. **لوب متتابع بلا تقسيم ولا قفل ولا لقطة** (Standards #43) وبلا شريط تقدّم
6. **الـ endpoint ده مش مذكور في `CLAUDE.md`** خالص — مش في جدول الـ Endpoints
   ولا في «المضبوط فعليًا في الداشبورد»

**قرار محتاج أحمد:** الميزة دي إما تتنقل للـ Worker (endpoint `fulfill_orders`
+ السر في Secrets + `writeLog` بـ `type` جديد يتسجّل في `constants` §7 الأول)،
أو **تتشال** لو مابقتش مستخدمة. النقل ده أكبر من مجرد مطابقة معيار — هو تغيير
في سطح الأداة.

---

### 🔴 ٧. صفر تهريب قبل ٢٠ موضع `innerHTML`

مفيش `esc()` في الملف، و٢٠ موضع بيبنوا `innerHTML` من قيم جاية من شوبيفاي أو
من إدخال الموظف: `product_title` · `notes` (نص حر بيكتبه الموظف) ·
`e.message` · أسماء الموظفين.

`<` واحدة في ملاحظة أو في اسم منتج **بتبلع باقي الصف أو تكسر الجدول** من غير أي
رسالة. وفي `renderDropdown` أسوأ:

```js
onclick='selectVariant(JSON.stringify(v).replace(/'/g,"&#39;"))'
```

الـ `JSON.stringify` بينتج `"` جوّه attribute محدود بـ `'` — أي منتج فيه
علامة تنصيص بيكسر الـ `onclick` كله، والضغط على الصف **مابيعملش حاجة**.

**الإصلاح:** `§CONFIG::esc` من `templates/21_app-shell.html` + تمريرها على كل
قيمة خارجية (شامل `title="..."`)، وتمرير الـ variant بـ `data-` attribute أو
index بدل `JSON.stringify` جوّه `onclick`.

---

## 🟠 القسم الثاني — بنية تحتية إلزامية ناقصة

| # | البند | المكان | الأثر |
|---|---|---|---|
| ٨ | **`?action=diag` غير موجود** | Worker | مفيش فحص ذاتي — binding أو سر ناقص مابيبانش غير بفشل عملية حقيقية |
| ٩ | **`?action=get_config` غير موجود** | Worker | مفيش رقم نسخة يترجع |
| ١٠ | **`MIN_WORKER_VERSION` + `cmpVersion` + `verStaleBtn`** | الواجهة | Promote ناقص أو rollback = الواجهة بتنادي endpoint مش موجود وبتقول «خطأ في الاتصال» |
| ١١ | **`assertEnv()` / `requireLocationId()`** | Worker | `LOCATION_ID` ناقص → `gid://shopify/Location/undefined` جوّه الميوتيشن. الأداة **أداة مخزون** — ده الفخ المكتوب في `CLAUDE.md` نفسه |
| ١٢ | **حارس `WORKER_SECRET` الغايب** | Worker | المقارنة الحالية (`secret !== env.WORKER_SECRET`) **آمنة فعلاً** — مش القالب المكسور — بس مفيش رسالة مفهومة، السر الناقص بيدّي 401 على كل حاجة بلا سبب |
| ١٣ | **أربع حالات النتيجة + `extra.result`** | الطرفين | الأداة بترجّع `ok:true` بس. مفيش `warning` (البند ١ فوق محتاجها) ومفيش `already`. ونتيجة مباشرة: **استعلام خط الأساس في `CLAUDE.md` بيفلتر على `type` لوحده** فبيقيس المحاولات مش الكتابة (`constants` §12) |
| ١٤ | **مهلة على `apiRequest`** | الواجهة | `get_audit_priorities` بيلفّ على **كل** variants المتجر ٢٥٠ في المرة. من غير `AbortController` الـ spinner ممكن يفضل دقايق — والموظف بيعمل refresh وسط عملية كتابة |
| ١٥ | **`orderId` الرقمي في `get_details`** | Worker | بيرجّع GID بس، فالواجهة مش قادرة تعمل `orderLink()` — وفعلاً بتعرض `#<id>` كنص عادي في نافذة التأكيد (Standards #20) |

**وبند إضافي:** `getLogs` فيها `Math.min(parseInt(...), 500)` بلا حراسة —
`limit` بقيمة مش رقمية بتدّي `NaN` وبيوصل لـ D1 كـ bind فيرجّع خطأ غامض.
(البند ده 🔴 في `worker-builder` v3.0.0.)

---

## 🟡 القسم الثالث — مطابقة معايير الواجهة (UI Standard v2)

### الطبقة البصرية

| البند | الحالي | المطلوب |
|---|---|---|
| `--container-max` | غير موجود · `.container{max-width:1100px}` | توكن في `:root` بواحدة من ٣ Tiers. الأداة فيها جدول أولويات كثيف + Log Tab → **L = 1400px** (والـ Log Tab لوحده بيمنع Tier S) |
| الخط | `IBM Plex Mono` محمّل في `<head>` ومستخدم | ملغي 19-08-2026 — `--font-mono` بنفس قيمة `--font-body` |
| أسماء التوكنز | `--font` · `--mono` | `--font-body` · `--font-mono` (Standards #36) |
| توكنز ناقصة | مفيش `--on-accent` · `--accent-border` · `--text-mid` · `--surface-hover` · `--green-dark` · `--red-dark` · `--amber-dark` | التوكنز الـ ١١ الجديدة تتنسخ في `:root` |
| `#hex` حرفي خارج `:root` | **٤٨ موضع** | صفر — الاستثناء الوحيد `rgba()` بتاعة الظلال (Standards #35) |
| أنواع التوست | `.toast.error` و`.toast.success` بس | الأربعة معرّفين: `success` · `error` · `warning` · `neutral`. **٢٥ نداء بـ `'error'` مقابل ٦ بـ `'success'`** — وكتير منهم تحذيرات («لا يوجد منتج بهذا الباركود» مش خطأ). وأول نداء بـ `'warning'` دلوقتي = **توست بلا خلفية** (Standards #41) |
| z-index | login 400 · modals 500 · toast 999 | 500 / 600 / 9999. ⚠️ **مفيش عطل فعلي هنا** — الترتيب النسبي سليم والمودالات فوق الدخول. مخالفة معيار مش باج |

### التابات والنوافذ

- `.main-seg` / `.main-seg-btn` → **مهجور**، البديل `.main-tabs-bar` / `.main-tab-btn` (عرض كامل + إطار على النشط)
- `.modal-close-btn` → `.modal-close-x` (إطار أحمر + خلفية حمراء)
- About + Changelog بعرض ثابت (`max-width:500px`) → `calc(var(--container-max) * .9)` وهيدر ثابت عند السكرول
- مودال About بيستخدم **كل** الكلاسات المهجورة: `.about-summary` · `.about-desc` ·
  `.about-label` · `.about-steps` · `.about-num` · `.about-endpoints` · `.about-ep` ·
  `.about-code` → البديل `.about-sec` / `.about-sec-body` + `<h4>` + `<ol>` + `<table>`
- مفيش Freeze on Scroll (اختياري — مربوط بشريط التابات الجديد)

### شاشة الدخول

- `<label for="loginSelect">الموظف</label>` → **يتشال** (Standards #34)
- `<option>— اختر —</option>` → `اختر اسمك للمتابعة`
- `#loginSub` بيتملي في `mode='employee'` → يبقى فاضي
- `loginStatus` بيقول «اختر الموظف للمتابعة.» بعد تحميل ناجح → يبقى فاضي
- مفيش `#loginVersionText` تحت زرار الإعدادات (Standards #24)
- `updateLoginSettingsBtnState()` → الاسم الصح `updateLoginSettingsBtn()` (بدون `State`)
- ✅ **سليم:** الشاشة على خطوتين فعلاً · حارس الكيبورد بيقرا `settingsOverlay` صح ·
  `isLoginBusy` · auto-submit 140ms · shake + 760ms · `currentEmployee` في الذاكرة بس

### رقم النسخة

مكتوب **٣ مرات منفصلة** (`<title>` · زرار الهيدر · بادج الـ Changelog) —
المطلوب مصدر واحد `TOOL_VERSION` + `renderVersionUI()` (Standards #15).

### الإعدادات

| الحقل | القرار |
|---|---|
| `Worker URL` | **يتشال** — يبقى `const WORKER_URL` في `§CONFIG` (Standards #28) |
| `Admin Worker URL` | **يتشال** — `const ADMIN_WORKER_URL` من `constants` §5b |
| `Worker Secret` | **يفضل** — السر الوحيد المسموح في `localStorage` |
| `Flow Companion Token` | مربوط بالبند 🔴 ٦ — قرار أحمد |
| زرار `🩺 افحص الأداة والاتصالات` | **ناقص** — إلزامي لأي أداة بتكتب |

✅ مفيش `placeholder` على أي حقل إعدادات — سليم.

### الجداول (تاب السجل + تاب الأولويات)

- السجل **بطاقات** (`.log-entry`) → المطلوب **جدول** (Standards #26)
- مفيش `.unified-section` (كارت واحد للفلاتر + الجدول)
- كل الفلاتر `<select>` قيمة واحدة → المطلوب **multi-select checkbox dropdown**
  بدون استثناء، و«مسح الاختيار» **فوق** القائمة (Standards #21)
- مفيش ترتيب على أعمدة (٣ حالات) — Standards #25
- «فلتر: N» → الاسم المعتمد **«النتائج»** في chip مربّع (Standards #24)
- ترتيب صف فلتر التاريخ: الفترة السريعة **آخر عنصر** → المطلوب **أول عنصر
  أقصى اليمين**، بعده «من» ثم «إلى» (Standards #19)
- `<input type="date">` بلا `onclick="this.showPicker&&this.showPicker();"`
- صيغة التاريخ+الوقت `DD-MM-YYYY الساعة HH:MM` → `📅 DD/MM/YYYY 🕐 HH:MM` (Standards #14)
- ✅ ExcelJS 4.3.0 للتصدير · ✅ `toLocaleString('en-US')` في كل مكان · ✅ زرار مسح الفلاتر أحمر

### السكانر

النمط الحالي **مش** الفرع الميّت بتاع v7.0.0 — هنا `gap` بيتقاس من الوقت الفعلي
بين الضغطات، فالفرع السريع (150ms) **بيتحقق فعلاً**. بس:

- الشكل المعتمد `isBurst = value.length > 3` **حتمي**، بينما القياس بالوقت
  بيعتمد على استجابة الـ main thread — أثناء رسم جدول الأولويات (آلاف الصفوف)
  الأحداث بتتكدّس والفجوة المقيسة بتعدّي 80ms فيرجع لـ 400ms في صمت
- **مفيش `§SCAN::plausibility`** — أي قيمة مسحوبة بتروح للـ API زي ما هي.
  باركودين اتلزقوا = رقم طويل يرجع «لا يوجد منتج بهذا الباركود» — **رسالة
  كاذبة على منتج موجود** (Standards #44 + Step 5)
- ✅ المسح جوّه `handleScan` مش في الـ listener — سليم

### التأكيد قبل الأفعال

✅ **البند ده منفّذ كويس فعلاً** — `openConfirm` بيعرض SKU والموظف والكمية
الحالية والتعديل والكمية الجديدة بالأرقام، مش «متأكد؟». الناقص بس إن زرار
التأكيد على فعل مالوش رجعة المفروض `.btn-red` (Standards #38).

---

## 📄 القسم الرابع — التوثيق والبصمة

| البند | الحالة |
|---|---|
| `// skills:` في `index.js` | **غير موجود خالص** — مخالفة القاعدة ٨ في `worker-builder` |
| `<!-- skills: -->` في `index.html` | موجود بس بقيم قديمة: `html-builder v1.0.0 · constants v1.2.0` |
| جدول البصمة في `CLAUDE.md` | v1.0.0 / v1.0.0 / v1.2.0 → الصح v3.0.0 / v7.0.0 / v2.1.0 |
| استعلام خط الأساس في `CLAUDE.md` | بيعدّ `type` لوحده → بيقيس **المحاولات**. الصح `AND json_extract(extra,'$.result')='success'` — وده متوقّف على تنفيذ البند 🟠 ١٣ |
| معيار §MD | `README.md` و`CLAUDE.md` **الاتنين** بلا `<div dir="rtl">` ولا بادج نسخة ولا فوتر «آخر تحديث» |
| Flow Companion | غير مذكور في جدول الـ Endpoints في `CLAUDE.md` رغم إنه نداء خارجي بيكتب |
| `type` المسجّلة في `constants` §7 | `ok` · `adjustment` · `login` · `logout` — مطابقة للكود ✅. ⚠️ أي `type` جديد (زي `rejected`) **لازم يتسجّل في `constants` §7 قبل أول نشر** |

---

## ترتيب التنفيذ المقترح

الترتيب ده مقصود: كل مرحلة بتقف على اللي قبلها، والأولى بتوقف نزيف فعلي.

| المرحلة | المحتوى | ليه هنا |
|---|---|---|
| **١ — Worker، عاجل** | البنود 🔴 ١ و٢ · `diag` + `get_config` · `assertEnv`/`requireLocationId` · `logged:false` | البند ١ بيحرّك مخزون ويقول «فشل». ده الوحيد اللي بينتج **ضرر مادي متكرر** |
| **٢ — Worker، السجل** | `Log Filter Model v2` كامل + `buildLogFilterSQL` + `orderByClause` + حراسة `parseInt` + استثناء login/logout في SQL | البند 🔴 ٣ نصّه في الـ Worker، والواجهة مش هتقدر تتصلّح من غيره |
| **٣ — الواجهة، الحراسات** | `esc()` · `apiRequest` بمهلة · `Intl` للتوقيت · نقل `loginAlert` · `MIN_WORKER_VERSION` · `§SCAN::plausibility` | حراسات مستقلة عن الشكل — بتتصلّح من غير إعادة بناء |
| **٤ — الواجهة، المعايير** | التوكنز · z-index · التابات · About · شاشة الدخول · `TOOL_VERSION` · الإعدادات | إعادة بناء الطبقة البصرية — أكبر شغلة وأقلها خطورة |
| **٥ — الجداول** | `.unified-section` · السجل كجدول · multi-select · الترتيب · الصفحات | بيقف على المرحلتين ٢ و٤ |
| **٦ — التوثيق** | البصمات · §MD · استعلام خط الأساس · قرار Flow Companion | آخر خطوة، بعد ما يبقى فيه حاجة توصفها |

**قرارات محتاجة أحمد قبل البدء:**

1. **Flow Companion** — ينتقل للـ Worker ولا يتشال؟
2. **`Admin Worker URL` و`Flow Token`** — لسه مستخدمين فعليًا؟
3. **Tier العرض** — L (1400px) هو المقترح لجدول الأولويات، تمام؟
4. **`ALLOWED_ORIGINS`** — بند التنظيف المفتوح من نقل 27-08-2026 لسه محتاج تأكيد.

---

## ملاحظات على حاجات **سليمة** (عشان ما تتلمسش من غير داعي)

- منطق `getAllVariantsForAudit` — الاستثناءات الأربعة (ACTIVE · SKU · tracked ·
  Suspended) مكتوبة ومعلّقة كويس
- `getUnfulfilledOrders` — اكتشاف مرحلة S1/S2 و`returnedIds` و`fulfillableQuantity`
  منطق دقيق ومتوافق مع `ecommoda-order-lifecycle`
- `wrangler.toml` — مطابق للقالب بالكامل: `name` صح · `DB` binding · `[vars]`
  فيها `SHOP_DOMAIN` و`LOCATION_ID` · تعليقات الفخاخ في مكانها
- CORS صارمة (أداة كتابة) — القرار صح
- `verifyEmployee` / `checkEmployee` / `registerPin` / `writeLog` — منسوخين
  حرفيًا من `§SHARED` بدون تعديل
- API version `2026-01` · `date_time` (مش `datetime`) · PIN في الـ body ·
  مفيش Session Tokens · D1 للسجل مش KV — كلها سليمة

---

آخر تحديث: 12-09-2026 — 15:07

</div>
